// async function userDetail(params){
//   const comment = await fetch("userComment");
//   const photos = await fetch("userPhoto");
//   const chat = await fetch("userChat");

// }

// //to run them parallely
// async function userDetail(params){
//  const [comment,photos,chat]= await Promise.all([fetch("userComment"),fetch("userPhoto"),fetch("userChat")]);

// }